#!/bin/bash

#echo -e "$(tput bold).aliases$(tput sgr0)"

alias g++='g++ -std=c++11'
alias clang='clang -std=c++11'

# I think zsh aliases these
#alias ls='ls -h'
#alias lsa='ls -A'
#alias ll='ls -l'
#alias lla='ls -lA'
